class Course {
    constructor(name, pointsQuantity, percentage) {
        this.name = name,
            this.pointsQuantity = pointsQuantity,
            this.percentage = percentage;
    }
}
export default Course